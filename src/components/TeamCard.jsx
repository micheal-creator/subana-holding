import { ArrowUpRight, Instagram, Linkedin } from 'lucide-react'
import { Link } from 'react-router-dom'
import Reveal from '../lib/Reveal.jsx'
export default function TeamCard({ member, index = 0 }) { const openSocial = (e, url) => { e.preventDefault(); e.stopPropagation(); window.open(url, '_blank', 'noopener,noreferrer') }; return <Reveal delay={index * 70}><Link to={`/team/${member.id}`} className="group block"><div className="relative aspect-[4/3] overflow-hidden rounded-lg bg-soft transition-colors group-hover:bg-teal"><img src={member.image} alt={`Portrait of ${member.name}`} className="absolute inset-0 h-full w-full object-cover grayscale-[.15] transition duration-500 group-hover:scale-105 group-hover:opacity-45" /><div className="absolute inset-0 bg-gradient-to-t from-navy/90 via-transparent to-transparent opacity-80" /><div className="relative flex h-full flex-col justify-between p-6"><span className="grid h-16 w-16 place-items-center rounded-full bg-navy/90 font-display text-xl text-white group-hover:bg-white group-hover:text-navy">{member.initials}</span><div><h3 className="text-2xl text-white">{member.name}</h3><p className="mt-1 text-sm text-white/70">{member.role}</p><div className="mt-4 flex items-center gap-3"><span className="inline-flex items-center gap-1 text-sm font-bold text-gold">View profile <ArrowUpRight size={15} /></span>{member.instagram && <button type="button" onClick={(e) => openSocial(e, member.instagram)} aria-label={`${member.name} on Instagram`} className="text-white/80 hover:text-gold"><Instagram size={15} /></button>}{member.linkedin && <button type="button" onClick={(e) => openSocial(e, member.linkedin)} aria-label={`${member.name} on LinkedIn`} className="text-white/80 hover:text-gold"><Linkedin size={15} /></button>}</div></div></div></div></Link></Reveal> }
