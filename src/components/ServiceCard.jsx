import { ArrowUpRight, Layers, Handshake, MonitorSmartphone, TrendingUp } from 'lucide-react'
import { Link } from 'react-router-dom'
import Reveal from '../lib/Reveal.jsx'
const ICONS = { Layers, Handshake, MonitorSmartphone, TrendingUp }
export default function ServiceCard({ service, index = 0 }) { const Icon = ICONS[service.icon] || Layers; return <Reveal delay={index * 70}><Link to={`/services/${service.id}`} className="group card block p-6 transition-all hover:-translate-y-1 hover:border-teal"><div className="flex items-start justify-between"><span className="grid h-12 w-12 place-items-center rounded-full bg-soft text-teal"><Icon size={22} /></span><span className="text-3xl text-teal/30">0{index + 1}</span></div><h3 className="mt-12 text-2xl">{service.title}</h3><p className="mt-2 text-sm leading-relaxed text-muted">{service.short}</p><span className="mt-6 inline-flex items-center gap-1 text-sm font-bold text-teal">Explore service <ArrowUpRight size={15} /></span></Link></Reveal> }
