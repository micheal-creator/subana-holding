import { ArrowUpRight } from 'lucide-react'
import { Link } from 'react-router-dom'
import Reveal from '../lib/Reveal.jsx'
export default function ProjectCard({ project, index = 0 }) { return <Reveal delay={index * 70}><Link to={`/projects/${project.id}`} className="group block"><div className="relative flex min-h-[300px] flex-col justify-between overflow-hidden rounded-lg bg-navy p-6 text-white transition-transform duration-300 group-hover:-translate-y-1"><img src={project.image} alt="" className="absolute inset-0 h-full w-full object-cover opacity-55 transition duration-500 group-hover:scale-105 group-hover:opacity-70" /><div className="absolute inset-0 bg-gradient-to-t from-navy via-navy/40 to-transparent" /><span className="relative text-xs font-bold uppercase tracking-[.15em] text-white/75">{project.category} · {project.year}</span><div className="relative"><h3 className="text-3xl">{project.title}</h3><p className="mt-2 max-w-sm text-sm text-white/80">{project.summary}</p><span className="mt-5 inline-flex items-center gap-2 text-sm font-bold">View project <ArrowUpRight size={16} /></span></div></div></Link></Reveal> }
